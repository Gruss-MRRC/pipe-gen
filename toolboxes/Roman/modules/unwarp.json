{
  "moduleName": "unwarp",
  "inputs": [
    { "name": "DTI",
      "format": "DTI fitted",
      "required": true
    },
    { "name": "Field Map",
      "format": "FieldMap",
      "required": true
    }
  ],
  "outputs": [
    { "name": "DTI unwarped",
      "format": "DTI fitted",
      "required": true
    }
  ],
  "enclosedCommand": "/gmrrc/ALLSHARE/EAS/SOFTWARE/SCRIPTS/unwarp.script {unwarped tissue} {tissue} {B0 map} {fat shift direction} {BW per pixel}" 
}
