{
  "moduleName": "group FieldMap",
  "inputs": [
    { "name": "image",
      "format": "NIfTI",
      "required": true
    },
    { "name": "fat shift direction",
      "format": "arg",
      "required": true
    },
    { "name": "bandwidth per pixel",
      "format": "arg",
      "required": true
    }
  ],
  "outputs": [
    { "name": "FieldMap group",
      "format": "FieldMap",
      "required": true
    }
  ],
  "enclosedCommand": "/gmrrc/ALLSHARE/EAS/SOFTWARE/SCRIPTS/dtiFit.script {DTIMaps} {EPIDTI} {bvalues} {bvectors}" 
}
