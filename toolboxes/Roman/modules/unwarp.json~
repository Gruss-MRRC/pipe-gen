{
  "moduleName": "unwarp",
  "inputs": [
    { "name": "Field Map",
      "format": "FieldMap",
      "required": true
    },
    { "name": "EPI image",
      "format": "T2WEPI",
      "required": true
    }
  ],
  "outputs": [
    { "name": "Field Map registered",
      "format": "FieldMap",
      "required": true
    }
  ],
  "enclosedCommand": "/gmrrc/ALLSHARE/EAS/SOFTWARE/SCRIPTS/unwarp.script {unwarped tissue} {tissue} {B0 map} {fat shift direction} {BW per pixel}" 
}
