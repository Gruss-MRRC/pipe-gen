{
  "moduleName": "copyDICOM",
  "inputs": [
    { "name": "fromFile",
      "format": "DICOM",
      "required": true
    }
  ],
  "outputs": [
    { "name": "toFile",
      "format": "DICOM",
      "required": true
    }
  ],
  "enclosedCommand": "cp {fromFile} {toFile}" 
}
