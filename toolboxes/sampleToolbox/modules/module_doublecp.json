{
  "moduleName": "double cp",
  "inputs": [
    { "name": "fromText0",
      "format": "text",
      "required": true
    },
    { "name": "fromText1",
      "format": "text",
      "required": true
    }
  ],
  "outputs": [
    { "name": "toText0",
      "format": "text",
      "required": true
    },
    { "name": "toText1",
      "format": "text",
      "required": true
    }
  ],
  "enclosedCommand": "cp {fromText0} {toText0} ; cp {fromText1} {toText1}" 
}
