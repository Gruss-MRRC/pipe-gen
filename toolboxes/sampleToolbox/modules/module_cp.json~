{
  "moduleName": "cp",
  "inputs": [
    { "name": "fromText",
      "format": "Text",
      "required": true
    }
  ],
  "outputs": [
    { "name": "toText",
      "format": "Text",
      "required": true
    }
  ],
  "enclosedCommand": "cp {fromText} {toText}" 
}
