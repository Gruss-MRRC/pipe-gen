{
  "moduleName": "cp",
  "inputs": [
    { "name": "fromText",
      "format": "text",
      "required": true
    }
  ],
  "outputs": [
    { "name": "toText",
      "format": "text",
      "required": true
    }
  ],
  "enclosedCommand": "cp {flags} {fromText} {toText}" 
}
