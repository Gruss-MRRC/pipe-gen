{
  "moduleName": "mv",
  "inputs": [
    { "name": "fromText",
      "format": "text", 
      "required": true
    }
  ],
  "outputs": [
    { "name": "toText",
      "format": "text",
      "required": true
    }
  ],
  "enclosedCommand": "mv {fromText} {toText}" 
}
